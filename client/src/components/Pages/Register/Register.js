import React from 'react'

import {NavLink} from "react-router-dom";

const Register = () => (
    <div id='signup'>
        Signup
        <NavLink to='/'>
            back home
        </NavLink>
    </div>
);

export default Register
